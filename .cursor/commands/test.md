# Write Tests

Write tests for the selected code. Include:

1. Core behavior (happy path)
2. Edge cases (empty inputs, boundaries)
3. Error cases (invalid inputs, auth failures)

Guidelines:
- Use descriptive test names
- Prefer small, focused tests
- Mock external systems at the boundary

