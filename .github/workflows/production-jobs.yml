name: Production Deployment - Background Jobs

on:
  push:
    branches:
      - main
    paths:
      - packages/jobs/**
      - packages/db/**
      - .github/workflows/production-jobs.yml

jobs:
  deploy-production:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Check TypeScript
        run: bun run typecheck
        working-directory: packages/jobs

      - name: Deploy Background Jobs
        env:
          TRIGGER_ACCESS_TOKEN: ${{ secrets.TRIGGER_ACCESS_TOKEN }}
          TRIGGER_PROJECT_ID: ${{ secrets.TRIGGER_PROJECT_ID }}
          TRIGGER_PROJECT_REF: ${{ secrets.TRIGGER_PROJECT_REF }}
        run: bunx trigger.dev@4.3.0 deploy
        working-directory: packages/jobs
